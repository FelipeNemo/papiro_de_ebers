# Modelo e tracing (OpenAI por padrão no kickoff)
MODEL=GPT-3.5-turbo
LANGSMITH_TRACING=true
LANGSMITH_API_KEY=lsv2...

# Provedores (defina conforme usar)
OPENAI_API_KEY=sk-...

# Opção B: Variáveis discretas (Supabase)
# Preencha estes campos para montarmos a URL de conexão no código
DB_USER=postgres.gmtrkaisnlbxyncwvgqu
DB_PASSWORD=YOUR_PASSWORD
# Supabase PgBouncer (pooler) host — porta 6543
DB_HOST=aws-0-us-west-1.pooler.supabase.com
DB_PORT=6543
DB_NAME=postgres
# Requerido para Supabase (pooler): TLS
DB_SSLMODE=require

# Deploy
DEPLOY_TARGET=cloud  # ou selfhost
EMBED_MODEL=openai:text-embedding-3-small
